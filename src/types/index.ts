export type MorlisAPIOptions = {
    serverUrl: string
    appId: string
    masterKey: string
}
export interface ENSMetadata {
    name: string
    description: string
    url: string
    image_url: string
}
